% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{estimate_direct_effect}
\alias{estimate_direct_effect}
\title{Estimate a non-Markov direct effect}
\usage{
estimate_direct_effect(
  data,
  bandwidth,
  time,
  exposure = 1,
  reference = 0,
  scale = c("cumulative_hazard", "survival"),
  columns = NULL
)
}
\arguments{
\item{data}{A data frame containing intermediate and terminal event times,
event indicators, and exposure.}

\item{bandwidth}{One positive bandwidth used for both curves, or two
bandwidths corresponding to \verb{(exposure, reference)} and
\verb{(reference, reference)}.}

\item{time}{Equally spaced, strictly increasing evaluation grid.}

\item{exposure}{Exposure level being compared.}

\item{reference}{Reference exposure level.}

\item{scale}{Effect scale: \code{"cumulative_hazard"} or \code{"survival"}.}

\item{columns}{Optional named character vector mapping the roles
\code{intermediate_time}, \code{terminal_time}, \code{intermediate_event},
\code{terminal_event}, and \code{exposure} to columns in \code{data}.}
}
\value{
An \code{nm_effect} object containing the time-varying direct effect.
}
\description{
Compares the counterfactual curves for \eqn{(z_a,z_b)=(1,0)} and
\eqn{(0,0)} by default. Two bandwidths may be supplied so each
counterfactual curve can use its selected bandwidth.
}
\examples{
estimate_direct_effect(
  nmSurvMed:::nm_example_data(), bandwidth = 0.25,
  time = seq(0, 2, by = 0.1)
)
}
