% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_nmSurvMed}
\alias{plot_nmSurvMed}
\title{Plot survival and cumulative-hazard mediation effects}
\usage{
plot_nmSurvMed(
  x,
  file = NULL,
  width = 4000,
  height = 3000,
  res = 300,
  color = "blue",
  ci_alpha = 0.2,
  main_prefix = NULL,
  show_legend = FALSE
)
}
\arguments{
\item{x}{An \code{nmSurvMed} fit created with \code{scale = "both"}.}

\item{file}{Optional output filename ending in \code{.png} or \code{.pdf}. When
\code{NULL}, the figure is drawn on the active graphics device. The parent
directory must already exist; this function never creates directories.}

\item{width, height}{Output dimensions. For PNG these are pixels; for PDF
these are inches.}

\item{res}{PNG resolution in dots per inch.}

\item{color}{Color of the estimated effect curves.}

\item{ci_alpha}{Opacity of the confidence interval shading.}

\item{main_prefix}{Optional text appended to each panel title, such as a
data-set or subgroup name.}

\item{show_legend}{Logical; display a legend in each panel. The default is
\code{FALSE}.}
}
\value{
The input fit, invisibly.
}
\description{
Produces a paper-style 2 by 2 figure containing survival direct and
indirect effects in the first row and cumulative-hazard direct and indirect
effects in the second row. Pointwise bootstrap confidence intervals are
shown as shaded regions.
}
\examples{
data <- nmSurvMed:::nm_example_data(100)
time <- seq(0, ceiling(max(data$T2) * 10) / 10, by = 0.1)
fit <- nmSurvMed(
  data,
  T1 = "T1_hat", D1 = "D1",
  T2 = "T2_hat", D2 = "D2", Z = "Z",
  time = time, bandwidth = c(0.2, 0.3),
  folds = 2, bootstrap = 3, verbose = FALSE
)
plot_nmSurvMed(fit)
}
