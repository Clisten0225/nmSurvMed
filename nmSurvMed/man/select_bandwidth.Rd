% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandwidth.R
\name{select_bandwidth}
\alias{select_bandwidth}
\title{Select kernel bandwidths by stratified cross-validation}
\usage{
select_bandwidth(
  data,
  candidates,
  time,
  folds = 5L,
  exposure = 1,
  reference = 0,
  seed = 123,
  cores = 1L,
  columns = NULL
)
}
\arguments{
\item{data}{A data frame containing intermediate and terminal event times,
event indicators, and exposure.}

\item{candidates}{Positive candidate bandwidths.}

\item{time}{Equally spaced, strictly increasing evaluation grid.}

\item{folds}{Number of stratified cross-validation folds.}

\item{exposure}{Exposure level being compared.}

\item{reference}{Reference exposure level.}

\item{seed}{Random seed used to construct folds.}

\item{cores}{Number of parallel worker processes. Forked processes are used
on Linux and macOS; PSOCK workers are used on Windows.}

\item{columns}{Optional named character vector mapping the roles
\code{intermediate_time}, \code{terminal_time}, \code{intermediate_event},
\code{terminal_event}, and \code{exposure} to columns in \code{data}.}
}
\value{
An object of class \code{nm_bandwidth}. Its \code{selected} component gives
the maximizing bandwidth for each scenario and \code{scores} contains all
cross-validation scores.
}
\description{
Evaluates candidate bandwidths for the three counterfactual scenarios
needed by the natural direct and indirect effects: \verb{(exposure, exposure)},
\verb{(exposure, reference)}, and \verb{(reference, reference)}.
}
\examples{
example_data <- nmSurvMed:::nm_example_data()
example_time <- seq(
  0, ceiling(max(example_data$T2) * 10) / 10, by = 0.1
)
select_bandwidth(
  example_data, candidates = c(0.2, 0.3),
  time = example_time, folds = 2
)
}
