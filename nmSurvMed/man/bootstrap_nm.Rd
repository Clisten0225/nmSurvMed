% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_nm}
\alias{bootstrap_nm}
\title{Stratified bootstrap inference}
\usage{
bootstrap_nm(
  data,
  effect = c("direct", "indirect", "cf_hazard"),
  bandwidth,
  time,
  B = 500L,
  conf_level = 0.95,
  exposure = 1,
  reference = 0,
  scale = c("cumulative_hazard", "survival"),
  z_a = exposure,
  z_b = reference,
  seed = 123,
  cores = 1L,
  columns = NULL
)
}
\arguments{
\item{data}{A data frame containing intermediate and terminal event times,
event indicators, and exposure.}

\item{effect}{Curve to bootstrap: \code{"direct"}, \code{"indirect"}, or
\code{"cf_hazard"}.}

\item{bandwidth}{One positive bandwidth for \code{effect = "cf_hazard"}.
For a direct or indirect effect, supply one shared bandwidth or two
bandwidths in the order documented by \code{\link[=estimate_direct_effect]{estimate_direct_effect()}} or
\code{\link[=estimate_indirect_effect]{estimate_indirect_effect()}}.}

\item{time}{Equally spaced, strictly increasing evaluation grid.}

\item{B}{Number of bootstrap samples.}

\item{conf_level}{Pointwise confidence level.}

\item{exposure}{Exposure level being compared.}

\item{reference}{Reference exposure level.}

\item{scale}{Effect scale: \code{"cumulative_hazard"} or \code{"survival"}.}

\item{z_a, z_b}{Joint-intervention exposure values used only when
\code{effect = "cf_hazard"}.}

\item{seed}{Random seed.}

\item{cores}{Number of parallel worker processes. Forked processes are used
on Linux and macOS; PSOCK workers are used on Windows.}

\item{columns}{Optional named character vector mapping the roles
\code{intermediate_time}, \code{terminal_time}, \code{intermediate_event},
\code{terminal_event}, and \code{exposure} to columns in \code{data}.}
}
\value{
An object of class \code{nm_bootstrap} containing the original estimate,
bootstrap replicates, bootstrap standard errors, and pointwise percentile
confidence limits.
}
\description{
Resamples observations with replacement within \verb{(D1,D2)} event strata,
re-estimates the requested curve, and constructs pointwise percentile
confidence intervals.
}
\examples{
boot <- bootstrap_nm(
  nmSurvMed:::nm_example_data(), effect = "direct", bandwidth = 0.25,
  time = seq(0, 2, by = 0.1), B = 10, seed = 1
)
}
