% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-hazard.R
\name{estimate_cf_hazard}
\alias{estimate_cf_hazard}
\title{Estimate a counterfactual hazard}
\usage{
estimate_cf_hazard(data, z_a, z_b, bandwidth, time, columns = NULL)
}
\arguments{
\item{data}{A data frame containing intermediate and terminal event times,
event indicators, and exposure.}

\item{z_a}{Exposure value used for the transition hazards.}

\item{z_b}{Exposure value used for the intermediate-event distribution.}

\item{bandwidth}{Positive kernel bandwidth.}

\item{time}{Equally spaced, strictly increasing evaluation grid.}

\item{columns}{Optional named character vector mapping the roles
\code{intermediate_time}, \code{terminal_time}, \code{intermediate_event},
\code{terminal_event}, and \code{exposure} to columns in \code{data}.}
}
\value{
An object of class \code{nm_effect} with components \code{time}, \code{estimate},
\code{scale}, \code{effect}, and \code{contrast}. Here \code{estimate} contains hazard
increments, not a cumulative hazard.
}
\description{
Estimates the hazard increment curve under the joint intervention
\eqn{(z_a, z_b)} in a non-Markov illness-death model. The transition hazard
under \code{z_a} is combined with the distribution of the intermediate event
under \code{z_b}.
}
\examples{
fit <- estimate_cf_hazard(
  nmSurvMed:::nm_example_data(), z_a = 1, z_b = 0,
  bandwidth = 0.25, time = seq(0, 2, by = 0.1)
)
}
